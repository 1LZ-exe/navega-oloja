import {StyleSheet}from 'react-native'

const estilo = StyleSheet.create({

    titulo: {
        fontSize: 35,
        fontWeight: 'bold',
        marginTop: 20,
        color: '#ffffff',
      },
      subtitulo: {
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: 15,
        color: '#ffffff',
      },
      tituloCard: {
        fontSize: 15,
        fontWeight: 'bold',
        color: '#ffffff',
      },


})

export default estilo;